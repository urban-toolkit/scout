{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "title": "Comparison Schema",

  "type": "object",
  "required": ["key", "metric", "chart"],
  "additionalProperties": true,

  "properties": {
    "key": {
      "type": "array",
      "items": { "type": "string", "minLength": 1 },
      "minItems": 1
    },
    "metric": {
      "type": "string",
      "minLength": 1
    },

    "chart": {
      "type": "string",
      "enum": ["bar", "pie", "table"]
    }
  }
}
